apply plugin: 'java'
apply plugin: 'idea'

repositories() {
    mavenCentral()
}

dependencies {
    compile("org.springframework.boot:spring-boot-starter-web:1.2.4.RELEASE")
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.4'
}